%tr{:class => [player.position, player.try(:team).try(:rest_round_1) == CURRENT_ROUND ? "descansa" : ""]}
  %td
    = player_image player
    = team_image player.team  
  %td
    = link_to player.name, player_path(player)
  - sum.each do |round|
    %td= round